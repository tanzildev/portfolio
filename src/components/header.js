import React from "react"

const Header = () => (
  <header
    style={{
      background: `rebeccapurple`,
    }}
  >
 
  </header>
)


Header.defaultProps = {
  siteTitle: ``,
}

export default Header
